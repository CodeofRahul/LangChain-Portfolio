- Create a new venv

`python
python -m venv venv
`

- activate venv

`python
venv\Scripts\Activate
`

- install packages from requirements.txt

`python
pip install -r requirements.txt
`
- run the code

`python
python filename.py
`
